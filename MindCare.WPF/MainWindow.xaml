<Window x:Class="MindCare.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MindCare - Inteligência Emocional Corporativa" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#8B0000" Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🧠 MindCare" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="Inteligência Emocional Corporativa" FontSize="16" Foreground="White" 
                           VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Navigation -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="#2C2C2C" BorderBrush="#444" BorderThickness="0,0,1,0">
                <StackPanel Margin="10">
                    <Button Content="📊 Dashboard" Height="50" Margin="0,10,0,5" 
                            Click="DashboardButton_Click" Background="#8B0000" Foreground="White" 
                            BorderThickness="0" FontSize="14" FontWeight="Bold"/>
                    <Button Content="👥 Monitoramento" Height="50" Margin="0,5,0,5" 
                            Click="MonitoringButton_Click" Background="#444" Foreground="White" 
                            BorderThickness="0" FontSize="14"/>
                </StackPanel>
            </Border>

            <!-- Content Area -->
            <Frame x:Name="ContentFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>

