<UserControl x:Class="MindCare.WPF.Views.MonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:MindCare.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </UserControl.Resources>
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="👥 Monitoramento de Funcionários" FontSize="28" FontWeight="Bold" 
                   Margin="20,20,20,10" Foreground="#8B0000"/>

        <!-- Filters -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="15" Margin="20,0,20,20" 
                BorderBrush="#DDD" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filtrar por funcionário:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="EmployeeComboBox" Width="300" SelectionChanged="EmployeeComboBox_SelectionChanged"/>
                <Button Content="🔄 Atualizar" Click="RefreshButton_Click" Margin="20,0,0,0" 
                        Background="#8B0000" Foreground="White" BorderThickness="0" Padding="15,5"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="2" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Employees List -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,10,0" 
                    BorderBrush="#DDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Funcionários" FontSize="18" FontWeight="Bold" 
                               Foreground="#8B0000" Margin="0,0,0,15"/>
                    <DataGrid x:Name="EmployeesDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None"
                              RowBackground="White" AlternatingRowBackground="#F9F9F9"
                              SelectionChanged="EmployeesDataGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Departamento" Binding="{Binding Department}" Width="*"/>
                            <DataGridTextColumn Header="Cargo" Binding="{Binding Position}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Metrics -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="20" Margin="10,0,0,0" 
                    BorderBrush="#DDD" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Métricas de Saúde" FontSize="18" FontWeight="Bold" 
                               Foreground="#8B0000" Margin="0,0,0,15"/>
                    <DataGrid x:Name="MetricsDataGrid" AutoGenerateColumns="False" 
                              IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="None"
                              RowBackground="White" AlternatingRowBackground="#F9F9F9">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="*"/>
                            <DataGridTextColumn Header="Valor" Binding="{Binding Value}" Width="100"/>
                            <DataGridTextColumn Header="Unidade" Binding="{Binding Unit}" Width="80"/>
                            <DataGridTextColumn Header="Data" Binding="{Binding RecordedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                            <DataGridTextColumn Header="Status" Width="100">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="IsAbnormal">
                                        <Binding.Converter>
                                            <converters:BoolToStatusConverter/>
                                        </Binding.Converter>
                                    </Binding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>

